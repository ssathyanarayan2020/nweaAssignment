{
  "name": "flatten-array",
  "version": "1.0.0",
  "description": "Given a nested list of integers, implement a function to flatten it",
  "main": "flattenIntegers.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC"
}
